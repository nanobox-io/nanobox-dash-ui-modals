#delete-verification-modal.modal
  .cancel-x
    img.shadow-icon(data-src="cancel-x-btn")
  
  .header= header
  .content !{content}
  .input
    input(placeholder="type '#{token}' to confirm")

  //- 
  .delete-zone
    .btn.cancel(href="#") Cancel
    .btn.delete.lifecycle.disabled Delete
